# Provisionando servidor

## Requisitos

### Ansible

Para MacOS

```
brew install ansible
```

## Executar Playbook

Na raiz do projeto, renomeie o arquivo `main.yml.example` para `main.yml` e execute o playbook `main.yml`.

```
time ansible-playbook main.yml
```
 